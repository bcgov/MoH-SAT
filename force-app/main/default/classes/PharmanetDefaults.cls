public with sharing class PharmanetDefaults implements AdjudicationAction {
    public void execute(SpecialAuthorityRequest sar, StepAction actionParams) {
        system.debug('EXEC: Pharmanet defaults.');
    }
}
